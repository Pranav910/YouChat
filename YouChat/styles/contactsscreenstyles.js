import { StyleSheet } from "react-native";
import { DARK_THEMED_BODY } from "../utils/colors";

export default styles = StyleSheet.create({
    main : {
        backgroundColor : DARK_THEMED_BODY,
        height : '100%'
    }
})