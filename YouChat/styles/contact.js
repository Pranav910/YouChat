import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    main : {
        display : 'flex',
        flexDirection : 'row',
        width : '100%',
        alignItems : 'center',
        gap : 20,
        margin : 10
    }
})